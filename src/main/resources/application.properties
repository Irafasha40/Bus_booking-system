spring.application.name=busbooking

# PostgreSQL DB settings - use environment variables
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d5f0942li9vc73d8np1g-a.oregon-postgres.render.com:5432/busystem}
spring.datasource.username=${DATABASE_USER:busystem_user}
spring.datasource.password=${DATABASE_PASSWORD:Xft60UOEx6p2M4lOB5Vrb99xXy5iTPEI}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false

# HikariCP connection pool (for better connection handling)
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.initialization-fail-timeout=-1

# CORS - Frontend URL (use environment variable)
frontend.url=${FRONTEND_URL:http://localhost:4200}

# JWT Secret for authentication
jwt.secret=${JWT_SECRET:MySuperSecureJwtSecretKeyThatIsAtLeast32Bytes!}

# Mail settings
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:rukuundoo@gmail.com}
spring.mail.password=${MAIL_PASSWORD:fdrv odlt xruh fkov}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
